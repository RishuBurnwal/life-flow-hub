# Offline AI Models

Place local model files here for strict offline mode.

Expected model path for current default:
- public/models/Xenova/phi-2/

At minimum this folder should contain tokenizer/config/model files required by transformers.js.

No remote model downloads are used when offline mode is enabled.
